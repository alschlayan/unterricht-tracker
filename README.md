# Unterricht Tracker (MVP)

Einzelbenutzer-Web-App zur Erfassung von Unterrichtseinheiten.
Deutschsprachige Oberfläche.

## Start
docker-compose up -d
cd backend && mvn spring-boot:run
cd frontend && npm install && npm run dev
